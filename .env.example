API_TOKEN=TOKEN_RAHASIA_LARAVEL

# ─── LLM API Keys ─────────────────────────────────────────────────────────────
# WAJIB: Minimal salah satu harus diisi untuk fitur AI Insights (weekly cronjob).
# Gemini diutamakan sebagai primary, OpenAI sebagai fallback.
# Jika keduanya kosong, endpoint /api/insights/generate akan return error 503.
#
# Flow retry: Gemini (2x retry) → OpenAI (2x retry) → Error 502
GEMINI_API_KEY=
GROQ_API_KEY=